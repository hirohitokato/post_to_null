{
  "name": "post_to_null",
  "version": "1.0.0",
  "description": "Simple server that accepts POST /dev/null and discards data while logging file uploads",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.24.0",
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.0.2"
  }
}
